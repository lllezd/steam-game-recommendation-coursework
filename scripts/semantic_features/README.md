# Semantic Features Scripts

The semantic feature experiments in this clean repository are represented by notebooks in `notebooks/semantic_features/`. The generated embedding matrices and CatBoost binary model files are intentionally excluded from GitHub.